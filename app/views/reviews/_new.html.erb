<h1>Adiciona uma nova review para <%= @coffee.name %></h1>
<%= simple_form_for([@coffee, @review], remote: true) do |f| %>
  <%= f.input :content %>
  <%= f.input :rating %>
  <%= f.submit class: 'btn btn-primary' %>
<% end %>
