<h1>Add a new review for <%= @coffee.name %></h1>
<%= simple_form_for([@coffee, @review], remote: true) do |f| %>
  <%= f.input :content %>
  <%= f.input :rating %>
  <%= f.submit class: 'btn btn-primary' %>
<% end %>
